export default class primeFactors {

    static generate(a: number): [] {
        return [];
    }
    
    static toStrictEqual(a: number): Array<number>{
        var arrayzao: Array<number> = [];
        for(var i = 0; i <= a; i++){
            if(i != 0){
                arrayzao.push(i);
            }
        }
        return arrayzao;
    }
}